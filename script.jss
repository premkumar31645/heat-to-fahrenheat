function convert() {
  const calVal = Number(document.getElementById("input").value);
  const fahVal = calVal * (9 / 5) + 32;
  const result = document.getElementById("result");
  result.innerHTML = fahVal;
  let;
}
